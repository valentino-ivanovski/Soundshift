<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Soundshift</title>
    <meta name="theme-color" content="#7e2ffc">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:slnt,wght@-10..0,100..900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(-135deg, #7e2ffc, #26acff);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 100vh;
            padding-right: 20px;
        }

        #animated-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(270deg, #712ae5, #229be6);
            background-size: 200% 200%;
            animation: gradient 5s infinite;
            z-index: -1;
        }

        @keyframes gradient {
            0% {
                background-position: 100% 0;
            }

            50% {
                background-position: 0 100%;
            }

            100% {
                background-position: 100% 0;
            }
        }

        h1{
            font-size: 80px;
            font-weight: bold;
            color: #fff;
            margin: 0;
        }

        p{
            font-size: 30px;
            color: #eee;
            margin: 10px 0;
        }

        .login-register {
            position: absolute;
            top: 40px;
            right: 40px;
            display: flex;
            gap: 20px;
        }

        .button {
            padding: 13px 20px;
            background-color: transparent;
            color: #fff;
            text-decoration: none;
            border: 1.5px solid #fff;
            border-radius: 50px;
            font-size: 18px;
            font-family: Inter, Helvetica;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }

        .button:hover {
            background-color: #fff;
            color: #000;
        }

        .buttonLM{
            padding: 13px 20px;
            background-color: transparent;
            color: #fff;
            text-decoration: none;
            border: 1.5px solid #fff;
            border-radius: 50px;
            font-size: 18px;
            font-family: Inter, Helvetica;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
            margin-left: 20%;
            margin-top: 30px;
        }

        .buttonLM:hover {
            background-color: #fff;
            color: #000;
        }

        .welcome1 {
            font-family: 'Inter', sans-serif; /* Use Inter font */
            font-size: 100px;
            color: #fff;
            margin-left: 20%;
            transition: 0.5s;
            font-variation-settings:
    "slnt" -10;
        }

        .welcome1:hover{
            cursor: default;
            color: rgb(217, 217, 217);
        }

        .welcome2 {
            font-family: 'Inter', sans-serif; /* Use Inter font */
            font-size: 30px;
            color: #fff;
            margin-left: 20%;
            font-weight: 300;
        }

        .welcome2:hover{
            cursor: default;
        }

        @media screen and (max-width: 768px) {
            .welcome1 {
                font-size: 60px;
            }

            .welcome2 {
                font-size: 20px;
            }
        }
    </style>
</head>

<body>
    <div id="animated-bg"></div>
    <div id="welcomeContainer">
        <h1 class="welcome1" id="animated-text">Soundshift</h1>
        <p class="welcome2">Discover and share your favorite songs with others ✨</p>
        <a href="https://www.omfgdogs.com">
            <button class="buttonLM">Learn More</button>
        </a>
    </div>
    <div class="login-register">
        <a href="login" class="button">Login</a>
        <a href="register" class="button">Register</a>
    </div>
</body>

</html>